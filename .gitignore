# Local secret source. Never committed; load into fly with `fly secrets import < .env`.
.env
.env.*
*.credentials.json

# Local scratch space (e.g. downloaded .faff run evidence). Never committed.
.scratch/
